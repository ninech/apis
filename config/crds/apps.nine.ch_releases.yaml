---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (unknown)
  creationTimestamp: null
  name: releases.apps.nine.ch
spec:
  group: apps.nine.ch
  names:
    kind: Release
    listKind: ReleaseList
    plural: releases
    singular: release
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels.application\.apps\.nine\.ch/name
      name: APPLICATION
      type: string
    - jsonPath: .spec.forProvider.config.size
      name: SIZE
      type: string
    - jsonPath: .spec.forProvider.config.replicas
      name: REPLICAS
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.atProvider.releaseStatus
      name: STATUS
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A Release is created from a "successful" Build. It contains all
          the information needed to deploy the application.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          creationTimestampNano:
            description: CreationTimestampNano is the unix nano timestamp of the release
              when it was created. It can be used to order releases and find the most
              current one. It provides a higher accuracy than the normal kubernetes
              resource CreationTimestamp which only has a resolution down to a second.
            format: int64
            type: integer
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: A ReleaseSpec defines the desired Release state
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: ReleaseParameters define the desired Release state
                properties:
                  basicAuthSecret:
                    description: BasicAuthSecret references a local secret which contains
                      the basic auth credentials
                    properties:
                      name:
                        description: Name of the target.
                        type: string
                    required:
                    - name
                    type: object
                  build:
                    description: Build references the Build resource, that is the
                      Release source.
                    properties:
                      name:
                        description: Name of the target.
                        type: string
                    required:
                    - name
                    type: object
                  config:
                    description: Config contains all configurations from the various
                      configuration sources (project level, application level, etc)
                      merged into one.
                    properties:
                      enableBasicAuth:
                        description: EnableBasicAuth enables basic authentication
                          for the application
                        nullable: true
                        type: boolean
                      env:
                        description: Env variables which are passed to the app at
                          runtime.
                        items:
                          properties:
                            name:
                              type: string
                            value:
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                      port:
                        description: Port the app is listening on.
                        format: int32
                        nullable: true
                        type: integer
                      replicas:
                        description: Replicas sets the amount of replicas of the running
                          app. If this is increased, make sure your application can
                          cope with running multiple replicas and all state required
                          is shared in some way.
                        format: int32
                        nullable: true
                        type: integer
                      size:
                        default: ""
                        description: ApplicationSize defines the size of an application
                          and the resources that will be allocated for it.
                        enum:
                        - ""
                        - micro
                        - mini
                        - standard-1
                        - standard-2
                        type: string
                    type: object
                  image:
                    description: Image defines the image spec of the built image
                    properties:
                      digest:
                        description: Digest specifies the image digest to use
                        type: string
                      pullPolicy:
                        default: Always
                        description: PullPolicy specifies the image pull policy to
                          use
                        type: string
                      pullSecret:
                        description: PullSecret specifies a image pull secret name
                        type: string
                      registry:
                        description: Registry specifies the registry from where the
                          image should be pulled
                        type: string
                      repository:
                        description: Repository specifies the repository from where
                          the image should be pulled
                        type: string
                      tag:
                        description: Tag specifies the image tag to use
                        type: string
                    type: object
                  verifiedHosts:
                    description: VerifiedHosts are the hosts which have been verified
                      and can be used in the release
                    items:
                      type: string
                    type: array
                required:
                - build
                - config
                - image
                type: object
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: An ReleaseStatus represents the observed Release state
            properties:
              atProvider:
                description: ReleaseObservation are the observable fields of a Release
                properties:
                  releaseStatus:
                    description: Status describes the status of the Release
                    type: string
                  replicas:
                    description: Replicas describes the amount of rolled out replicas,
                      ie. for the underlying Deployment, it shows number of non-terminated
                      pods targeted by this Release.
                    format: int32
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            required:
            - atProvider
            type: object
        required:
        - creationTimestampNano
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
